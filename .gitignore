*.dll
*.exe
.DS_Store
example.tf
terraform.tfplan
*.tfstate
*.tfstate.*
bin/
dist/
modules-dev/
/pkg/
website/.vagrant
website/.bundle
website/build
website/node_modules
.vagrant/
*.backup
.terraform/
*.log
*.bak
*~
.*.swp
.idea
*.iml
*.test
*.iml
.cursor/
.vscode/
# Claude Code transient session state
.claude/scheduled_tasks.lock
.terraform.lock.hcl
samples/

website/vendor

# Test exclusions
!command/test-fixtures/**/*.tfstate
!command/test-fixtures/**/.terraform/

# Keep windows files with windows line endings
*.winfile eol=crlf

local/

# YBA license file used by acctest fixtures (secret)
*.lic

# GCP service-account key fetched for local acctest runs (secret)
*-creds.json
.gcp-creds.json

# Test env written by `make acctest-env` (secret: SA key + API token)
acctest/env*

# Local cross-repo development: a go.work that points platform-go-client at
# clients generated from a yugabyte-db working copy, so provider changes can be
# built against unreleased YBA APIs. Machine-specific; CI resolves go.mod.
go.work
go.work.sum
